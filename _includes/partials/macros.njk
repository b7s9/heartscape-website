{% macro pressImage(uri,description) %}
<div>
  <a href="{{uri}}" download>
    <img src="{{uri}}" alt="{{description}}">
  </a>
  <span class="mt-4 text-lg">{{description}}</span>
  {# <a href="{{uri}}" download class="inline-block px-4 py-2 bg-black text-white font-semibold md:text-lg rounded-lg transition-colors duration-200 hover:bg-gray-800 hover:shadow-md cursor-pointer">Download</a> #}
</div>
{% endmacro %}
